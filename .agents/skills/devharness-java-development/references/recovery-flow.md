# Recovery Flow

1. Run `scripts/memory-recover.sh --latest`.
2. Read `.agents/memory/exports/RECOVERY_CONTEXT.md`.
3. Continue from `pending_items`.
4. Read `next_read_files` before editing when present.
5. Verify assumptions from the previous checkpoint.
